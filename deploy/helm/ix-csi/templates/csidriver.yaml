apiVersion: storage.k8s.io/v1
kind: CSIDriver
metadata:
  name: {{ include "ix-csi.driverName" . }}
  labels:
    {{- include "ix-csi.labels" . | nindent 4 }}
spec:
  attachRequired: true
  podInfoOnMount: true
  fsGroupPolicy: File
  volumeLifecycleModes:
    - Persistent
    - Ephemeral
